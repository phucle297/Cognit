{
  "name": "@cognit/wrap",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo \"wrap: no build (consume TS source directly)\"",
    "typecheck": "tsgo --noEmit",
    "test": "vitest --run",
    "test:watch": "vitest",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@cognit/core": "workspace:*",
    "@cognit/db": "workspace:*",
    "@cognit/verification": "workspace:*",
    "effect": "3.21.3",
    "ulid": "^3.0.2"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "vitest": "^4.1.9"
  }
}
