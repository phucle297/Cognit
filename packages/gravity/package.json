{
  "name": "@cognit/gravity",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo \"gravity: no build (consume TS source directly)\"",
    "typecheck": "tsgo --noEmit",
    "test": "vitest --run",
    "test:watch": "vitest",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@cognit/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "vitest": "^4.1.9"
  }
}
