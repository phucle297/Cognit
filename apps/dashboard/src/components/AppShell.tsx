/**
 * AC-required path. Re-export of the FSD canonical AppShell
 * widget (src/widgets/app-shell/index.tsx). Feature-Sliced
 * Design: the widget layer owns the layout.
 */
export { AppShell } from "@/widgets/app-shell";
